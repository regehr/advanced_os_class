#include "user.h"
#include "types.h"




int main(void){
  
  unsigned long msec;
  unsigned long sec; 
  
  if(gettime(&msec,&sec)<0){
    printf("Error on gettime");
    exit();
  }
  printf("msec: %ul and sec: %ul\n"); 
  exit();
}
